<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_pressed="true">
        <layer-list>
            <!-- Background color (darker when pressed) -->
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="@color/darker_white" /> <!-- Adjust the color as needed -->
                    <corners android:radius="2000dp" />
                </shape>
            </item>
        </layer-list>
    </item>

    <!-- Default state -->
    <item>
        <layer-list>
            <!-- Background color (your existing button background) -->
            <item android:drawable="@drawable/primary_button" /> <!-- Replace with your existing button background -->
            <!-- Add shadow with transparency -->
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="@android:color/transparent" />
                    <corners android:radius="2000dp" /> <!-- Match your button corner radius -->
                </shape>
            </item>
        </layer-list>
    </item>
</selector>
